<!DOCTYPE html>
<html>
<head>
    <title>Edit Mountable Service - IRCAD Africa Intranet</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body>
    <div class="dashboard-container">
        <%- include('../partials/sidebar', { user: user, path: '/mountable-services' }) %>
        
        <main class="content">
            <div class="content-header">
                <h1>Edit Mountable Service</h1>
                <a href="/mountable-services" class="btn-new">
                    <i class="fas fa-arrow-left"></i> Back to Services
                </a>
            </div>

            <% if (error) { %>
                <div class="error-message"><%= error %></div>
            <% } %>

            <form action="/mountable-services/<%= service.id %>/edit" method="POST" class="form-container">
                <div class="form-group">
                    <label for="name">Service Name:</label>
                    <input type="text" id="name" name="name" value="<%= service.name %>" required>
                </div>

                <div class="form-group">
                    <label for="description">Description:</label>
                    <textarea id="description" name="description" rows="3" required><%= service.description %></textarea>
                </div>

                <div class="form-group">
                    <label for="windows_command">Windows Mount Command:</label>
                    <input type="text" id="windows_command" name="windows_command" value="<%= service.windows_command %>" required>
                    <small class="form-text text-muted">Use %USERNAME% for the current user's username</small>
                </div>

                <div class="form-group">
                    <label for="mac_linux_command">Mac/Linux Mount Command:</label>
                    <input type="text" id="mac_linux_command" name="mac_linux_command" value="<%= service.mac_linux_command %>" required>
                    <small class="form-text text-muted">Use $USER for the current user's username</small>
                </div>

                <div class="form-group">
                    <label for="icon">Icon Class:</label>
                    <input type="text" id="icon" name="icon" value="<%= service.icon %>" required>
                    <small class="form-text text-muted">Enter a Font Awesome icon class (e.g., fa-folder, fa-file, fa-image)</small>
                </div>

                <button type="submit" class="btn-submit">
                    <i class="fas fa-save"></i> Update Service
                </button>
            </form>
        </main>
    </div>
</body>
</html> 